See /docs for papers giving a formal description of the SigClust algorithm and for the official R documentation on the R function sigclust.

The folder enwiki_data contains a data set "data2.tsv"  of almost 20,000 wikipedia article revisions, and the utility "get_mat" in the module read.py allows one to easily extract a numpy arrayto pass to sigclust.
